class Solution:
    def getSneakyNumbers(self, nums: List[int]) -> List[int]:
        d = {}
        for n in nums:
            d[n] = d.get(n,0)+1
        ans  = [k for k,v in d.items() if v == 2]
        return sorted(ans)
